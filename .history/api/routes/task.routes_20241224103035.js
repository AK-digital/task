import express from "express";
const router = express.Router();
import * as authMiddleware

router.post("/");

router.get("/");

router.update("/");

router.delete("/");

router.patch("/");
router.patch("/");

export default router;
