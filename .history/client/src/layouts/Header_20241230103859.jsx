import { AuthContext } from "@/context/auth"

export default function Header() {
    const context = AuthContext()
    return (
        <header>
            <nav>

            </nav>
        </header>
    )
}