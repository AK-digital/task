"use server";
export default async function Project({ params }) {
  const { id } = await params;
  console.log(projectId);
  // const boards=
  return (
    <main>
      <div>{/* Boards */}</div>
    </main>
  );
}
